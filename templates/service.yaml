apiVersion: v1
kind: Service
metadata:
  name: {{ include "base-apps-web.fullname" . }}
  labels:
    {{- include "base-apps-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "base-apps-web.selectorLabels" . | nindent 4 }}
